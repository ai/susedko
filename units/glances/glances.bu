systemd:
  units:
    - name: glances.service
      enabled: true
passwd:
  users:
    - name: glances
      primary_group: glances
      shell: /bin/false
  groups:
    - name: glances
storage:
  files:
    - path: /usr/local/etc/domains/status.local
      mode: 0o444
      user:
        name: avahi
      group:
        name: avahi
      contents:
        inline: |
          61209
