systemd:
  units:
    - name: glances.service
      enabled: true
passwd:
  users:
    - name: glances
      primary_group: glances
      shell: /bin/false
  groups:
    - name: glances
storage:
  files:
    - path: /usr/local/etc/nginx/conf.d/susedko.local.conf
      mode: 0o444
      user:
        name: domains
      group:
        name: domains

