disabled: true
passwd:
  users:
    - name: vaultwarden
      primary_group: vaultwarden
      shell: /bin/false
      groups:
        - dockerhub
  groups:
    - name: vaultwarden
systemd:
  units:
    - name: vaultwarden.service
      enabled: true
storage:
  directories:
    - path: /var/mnt/vault/warden/
      mode: 0o600
      user:
        name: vaultwarden
      group:
        name: vaultwarden
