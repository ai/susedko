desc: Samba Server
waitOnline: true

user: share
group: share

podman:
  image: ghcr.io/crazy-max/samba:latest
  ports:
    - 1445:445
  volumes:
    - /var/mnt/vault/shared:/var/mnt/vault/shared:z
    - /var/mnt/vault/.config/samba/:/data/:z
